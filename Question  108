/* Q108 (Logic Enhancers)
Product of array except self.
*/
#include <stdio.h>
int main() {
    int n, i, j, prod;
    scanf("%d", &n);
    int arr[n], ans[n];
    for(i=0; i<n; i++) {
        scanf("%d", &arr[i]);
    }
    for(i=0; i<n; i++) {
        prod = 1;
        for(j=0; j<n; j++) {
            if(i != j) prod *= arr[j];
        }
        ans[i] = prod;
    }
    printf("[");
    for(i=0; i<n; i++) {
        printf("%d%s", ans[i], (i<n-1)?",":"");
    }
    printf("]");
    return 0;
}
