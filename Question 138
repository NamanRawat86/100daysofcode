/* Q138 (Enum)
Print all enum names and integer values using a loop.
*/
#include <stdio.h>
enum Colors {RED, GREEN, BLUE};
int main() {
    int i;
    for(i=RED; i<=BLUE; i++) {
        printf("%d ", i);
    }
    return 0;
}
