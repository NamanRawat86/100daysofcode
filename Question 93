/* Q93 (Strings)
Check if two strings are anagrams of each other.
*/
#include <stdio.h>
#include <string.h>
int main() {
    char s1[100], s2[100];
    int c1[26]={0}, c2[26]={0}, i, flag=1;
    scanf("%s %s", s1, s2);
    if(strlen(s1) != strlen(s2)) {
        flag = 0;
    } else {
        for(i=0; s1[i]!='\0'; i++) {
            c1[s1[i]-'a']++;
            c2[s2[i]-'a']++;
        }
        for(i=0; i<26; i++) {
            if(c1[i] != c2[i]) {
                flag = 0;
                break;
            }
        }
    }
    printf(flag ? "Anagrams" : "Not anagrams");
    return 0;
}
