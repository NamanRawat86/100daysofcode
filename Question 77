/* Q77 (2D Arrays)
Check if the elements on the diagonal of a matrix are distinct.
*/
#include <stdio.h>
int main() {
    int n, i, j, flag=1;
    scanf("%d %d", &n, &n);
    int mat[n][n], diag[n];
    for(i=0; i<n; i++) {
        for(j=0; j<n; j++) {
            scanf("%d", &mat[i][j]);
        }
        diag[i] = mat[i][i];
    }
    for(i=0; i<n; i++) {
        for(j=i+1; j<n; j++) {
            if(diag[i] == diag[j]) {
                flag = 0;
                break;
            }
        }
    }
    printf(flag ? "True" : "False");
    return 0;
}
