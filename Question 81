/* Q81 (Strings)
Count characters in a string without using built-in length functions.
*/
#include <stdio.h>
int main() {
    char s[100];
    int i=0;
    scanf("%s", s);
    while(s[i] != '\0') {
        i++;
    }
    printf("%d", i);
    return 0;
}
